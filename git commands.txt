#git init		*initializes a new git repository
#git status		*shows status of the contents of the working directory and staging area
#git add filename	*adds files from the working directory to the staging area
#git diff filename	*shows the difference between the working directory and the staging area
#git commit -m		*permanently stores file changes from the staging area in the repository
#git log		*shows a list of all previous commits
#git log --oneline	*shows log in one line
